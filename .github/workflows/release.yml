name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  deploy:
    runs-on: windows-latest

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Extract version from tag
        id: version
        shell: bash
        run: echo "version=${GITHUB_REF_NAME#v}.0" >> $GITHUB_OUTPUT

      - name: Publish ClickOnce
        run: |
          msbuild TrayCommander.csproj /restore /t:Publish /p:PublishProfile=ClickOnce /p:ApplicationVersion=${{ steps.version.outputs.version }} /p:GenerateClickOnceManifests=true /p:Configuration=Release /v:diag 2>&1 | Out-File build-diag.log -Encoding utf8
          if ($LASTEXITCODE -ne 0) {
            Get-Content build-diag.log | Select-String -Pattern 'MSB3094|MSB4803|UpdateManifest|_Deployment|GenerateClickOnce|error MSB' | Select-Object -First 80
            exit 1
          }

      - name: Upload diagnostic log
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-diagnostic
          path: build-diag.log

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./bin/Release/net10.0-windows/publish
